import React from "react";
import StyledMessage from "./StyledMessage"; // Import the StyledMessage component

function App() {
  return (
    <div>
      <StyledMessage /> {/* Render the styled message */}
    </div>
  );
}

export default App;
